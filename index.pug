:go:func(arg) items []string
doctype html
html(lang="en")
  head
    title Pinlist
    meta(charset="utf-8")
    script(src="lib.js")

  body
    h1 Pinlist
    form(action="javascript:")
      input(type="submit" onclick="add(this)" style="display:none")
      table
        each item in items
          tr
            td
              input(type="submit" value="🗑️" onclick="del(this)")
            td(id="item")
              | #{item}
      input(type="text" id="newItem" autofocus="")
      input(type="submit" value="➕" onclick="add(this)")
